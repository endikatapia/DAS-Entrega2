<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.das_entrega2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TemaDAS"
        android:usesCleartextTraffic="true">
        <activity android:name=".ActivityPedido"></activity>
        <activity android:name=".ActivityDetalles" />
        <activity android:name=".ActivityPostre" />
        <activity android:name=".ActivityPreferencias" />
        <activity android:name=".ActivityComida" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>